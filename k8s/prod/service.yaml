apiVersion: v1
kind: Service
metadata:
  name: devops-cicd-demo-service
  namespace: prod
spec:
  type: NodePort
  selector:
    app: devops-cicd-demo
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30082